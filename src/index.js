import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

ReactDOM.render(<App />, document.getElementById("root"));

// Component : Component is function that returns HTML.
// React is required if you want to write like HTML using component .
// This combination between javascript and HTML is called 'jsx'. jsx is HTML into javascript.
